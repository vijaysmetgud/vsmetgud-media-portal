server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /media/ {
        alias /usr/share/nginx/media/;
        autoindex on;
    }
}
